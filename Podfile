# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'sampleAutoLayoutLibrary' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for sampleAutoLayoutLibrary

pod 'PureLayout'
pod 'Firebase'
pod 'Firebase/Auth'
pod 'Firebase/Database'

    
  target 'sampleAutoLayoutLibraryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sampleAutoLayoutLibraryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
